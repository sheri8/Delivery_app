import 'package:flutter/material.dart';

import 'Account/profile.dart';

class Account123 extends StatelessWidget {
  const Account123({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Profile123(),
    );
  }
}
